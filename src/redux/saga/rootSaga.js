import {all, fork} from 'redux-saga/effects';
import {
  Logout,
  SignUp,
  SignIn,
  GetUserProfile,
  VerifyOtp,
  ResendOtp,
  PartnerCode,
  ResendPartnerCode,
  Backup,
  Deactivate,
  EditProfile,
  EnableBiometric,
  VerifyBiometric,
  AddTemplate,
  EditTemplate,
  GetTemplates,
  DeleteTemplate,
  AddTodoList,
  EditTodoList,
  DeleteTodoList,
  AddNote,
  EditNote,
  DeleteNote,
  GetOrganiseList,
  PinUnpin,
  GetChat,
  GetMedia,
  GetLinks,
  GetChatSearch,
  DeleteChat,
  GetProfile,
  AddPost,
  GetPost,
  AddComment,
  DeleteComment,
  DeletePost,
  DeleteReaction,
  AddReaction,
  AddFeelingsCheck,
  GetFeelingsCheck,
  AddAnswerQa,
  AddEvent,
  EditEvent,
  DeleteEvent,
  GetEvent,
  GetSpecialEvent,
  AddStory,
  AddHighlight,
  RemoveHighlight,
  GetStories,
  GetHighlights,
  createNewHighlightType,
  RemoveSingleHighlight
} from './watcherSaga';
import { DeleteStory } from '../actions';


export function* rootSaga() {
  yield all([
    fork(SignIn),
    fork(Logout),
    fork(SignUp),
    fork(VerifyOtp),
    fork(ResendOtp),
    fork(PartnerCode),
    fork(ResendPartnerCode),
    fork(GetUserProfile),
    fork(Backup),
    fork(Deactivate),
    fork(EditProfile),
    fork(EnableBiometric),
    fork(VerifyBiometric),
    fork(AddTemplate),
    fork(EditTemplate),
    fork(DeleteTemplate),
    fork(GetTemplates),
    fork(AddTodoList),
    fork(EditTodoList),
    fork(DeleteTodoList),
    fork(AddNote),
    fork(EditNote),
    fork(DeleteNote),
    fork(GetOrganiseList),
    fork(PinUnpin),
    fork(GetChat),
    fork(GetMedia),
    fork(GetLinks),
    fork(GetChatSearch),
    fork(DeleteChat),
    fork(GetProfile),
    fork(AddPost),
    fork(GetPost),
    fork(AddComment),
    fork(DeleteComment),
    fork(DeletePost),
    fork(DeleteReaction),
    fork(AddReaction),
    fork(AddFeelingsCheck),
    fork(GetFeelingsCheck),
    fork(AddAnswerQa),
    fork(AddEvent),
    fork(EditEvent),
    fork(DeleteEvent),
    fork(GetEvent),
    fork(GetSpecialEvent),
    fork(AddStory),
    fork(DeleteStory),
    fork(AddHighlight),
    fork(RemoveHighlight),
    fork(GetStories),
    fork(GetHighlights),
    fork(createNewHighlightType),
    fork(RemoveSingleHighlight)
    // fork(GetProducts),
  ]);
}
